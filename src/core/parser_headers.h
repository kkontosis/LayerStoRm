#pragma once

// Additional headers included by the generated config_parser.h.
// Add dependency-free headers here that define types referenced
// via x-cType / x-externalType in the config schema.

#include "core/gpu_ref.h"
