{
  "_comment": "EPM-4 grid (screening design around the default + the core-hypothesis cells; run by run_epm4_grid.py off epm4-real-base.json). c00/c16 are references (Tier-0 through the identical eval path; second seed of the default = noise gauge). Equal-params notes: uniform-ranks control pins min_rank=max_rank=9 at the same 58,060,800 delta budget (525 cells x 9 units x 12288 = 58,060,800 exactly); c10's shared-base-only bumps r_base 64->127 so total trainables stay ~equal to base+delta; c11's direct arm budget 85,900,000 makes its total trainables ~match the router arm's.",
  "cells": [
    {"name": "c00_tier0", "note": "Tier-0 reference (0 steps, prior taps)",
     "overrides": ["optim.max_steps=0"]},
    {"name": "c01_w_w", "note": "DEFAULT: W-loss x W-ranks (core)",
     "overrides": []},
    {"name": "c02_w_uniformranks", "note": "core: W-loss x uniform-ranks, equal params",
     "overrides": ["model.adapter.delta.min_rank=9",
                    "model.adapter.delta.max_rank=9"]},
    {"name": "c03_uniform_uniform", "note": "core: uniform-loss x uniform-ranks, equal params",
     "overrides": ["wmap={\"kind\":\"uniform\"}",
                    "model.adapter.delta.min_rank=9",
                    "model.adapter.delta.max_rank=9"]},
    {"name": "c04_w_w_nok1boost", "note": "core A/B: directive W without the k=1-early boost",
     "overrides": ["wmap.k1_early_layers=0"]},
    {"name": "c05_budget_quarter", "note": "rank budget 14.5M (quarter)",
     "overrides": ["model.adapter.delta.budget_params=14515200"]},
    {"name": "c06_budget_double", "note": "rank budget 116.1M (double)",
     "overrides": ["model.adapter.delta.budget_params=116121600"]},
    {"name": "c07_rbase_16", "note": "shared base rank 16",
     "overrides": ["model.adapter.r_base=16"]},
    {"name": "c08_rbase_128", "note": "shared base rank 128",
     "overrides": ["model.adapter.r_base=128"]},
    {"name": "c09_fusion", "note": "Tier-2 5-tap fusion (rank-16 per-tap residual)",
     "overrides": ["model.taps={\"mode\":\"fusion\",\"map\":\"prior\",\"init_gain\":4.0,\"rank\":16}"]},
    {"name": "c10_nodelta", "note": "shared-base only (no per-position deltas), r_base 127 for ~equal params",
     "overrides": ["model.adapter.delta.enabled=false",
                    "model.adapter.r_base=127"]},
    {"name": "c11_direct", "note": "direct low-rank heads (arm B), ~equal trainables",
     "overrides": ["model.arm=\"direct\"",
                    "model.direct_head.delta.budget_params=85900000"]},
    {"name": "c12_bce", "note": "BCE-primary loss",
     "overrides": ["loss={\"kl_weight\":0.0,\"kl_temperature\":1.0,\"bce_weight\":1.0,\"mse_weight\":0.0}"]},
    {"name": "c13_klT2", "note": "KL temperature 2.0",
     "overrides": ["loss.kl_temperature=2.0"]},
    {"name": "c14_standardize", "note": "standardize input norm",
     "overrides": ["model.normalization={\"kind\":\"standardize\"}"]},
    {"name": "c15_nonorm", "note": "no input norm",
     "overrides": ["model.normalization={\"kind\":\"none\"}"]},
    {"name": "c16_seed1", "note": "default at seed 1 (noise gauge)",
     "overrides": ["seed=1"]},
    {"name": "c17_direct_bce", "note": "follow-up: combine the two significant wins (direct heads x BCE-primary loss)",
     "overrides": ["model.arm=\"direct\"",
                    "model.direct_head.delta.budget_params=85900000",
                    "loss={\"kl_weight\":0.0,\"kl_temperature\":1.0,\"bce_weight\":1.0,\"mse_weight\":0.0}"]}
  ]
}
